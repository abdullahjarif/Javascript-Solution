const student = ["Jarif", "Nadiya", "Arif", "Jasia", "Mayad", "Raj"];

student.map(function(item,index){
    // console.log(`Index ${index} item ${item}`);
    // console.log(item);
    console.log(index, item);
    
});